"use client";

/** Data sources route (`/sources`) — two-column list + schema view. */

import { DataSourcesView } from "@/components/sources/DataSourcesView";

export default function SourcesPage() {
  return <DataSourcesView />;
}
