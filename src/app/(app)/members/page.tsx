"use client";

/** Members route (`/members`) — org member + invitation management (admins). */

import { MembersView } from "@/components/members/MembersView";

export default function MembersPage() {
  return <MembersView />;
}
