"use client";

/** Table Editor / visual query builder route (`/editor`). */

import { BuilderView } from "@/components/query/BuilderView";

export default function EditorPage() {
  return <BuilderView />;
}
