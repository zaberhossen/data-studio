"use client";

/** Logs route (`/logs`) — query-run history, Supabase-logs style. */

import { LogsView } from "@/components/history/LogsView";

export default function LogsPage() {
  return <LogsView />;
}
