"use client";

/** Saved queries route (`/saved`) — two-column templates + preview. */

import { SavedQueriesView } from "@/components/saved/SavedQueriesView";

export default function SavedPage() {
  return <SavedQueriesView />;
}
